import "./Article.scss";
import React from "react";
import {useRouteMatch} from "react-router";

export default function Article(props: any) {
  let routeMatch = useRouteMatch();
  // let {id} = routeMatch.params;
  //
  // console.log(id);

  return (
    <>

    </>
  );
}